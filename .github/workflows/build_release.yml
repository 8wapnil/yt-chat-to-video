name: Build and Release

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name for release (e.g. v1.0.1)'
        required: true
        default: 'manual-build'

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build with PyInstaller (Mac)
      if: runner.os == 'macOS'
      run: |
        pyinstaller --noconfirm --onedir --windowed --name "YTChatRender" \
          --add-data "fonts:fonts" \
          --add-data "yt-chat-to-video.py:." \
          gui.py
        # Zip the .app for release
        cd dist
        zip -r YTChatRender-Mac.zip YTChatRender.app

    - name: Build with PyInstaller (Windows)
      if: runner.os == 'Windows'
      run: |
        pyinstaller --noconfirm --onefile --windowed --name "YTChatRender" `
          --add-data "fonts;fonts" `
          --add-data "yt-chat-to-video.py;." `
          gui.py

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: YTChatRender-${{ runner.os }}
        path: |
          dist/*.zip
          dist/*.exe

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v4

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ inputs.tag_name }}
        files: |
          YTChatRender-macOS/*.zip
          YTChatRender-Windows/*.exe
        generate_release_notes: true
